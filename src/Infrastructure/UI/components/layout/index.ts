/**
 * UI LAYER - Layout Components
 * ─────────────────────────────────────────────────────────────────────────────
 * Componentes de estructura de página (shell, header, sidebar, footer).
 *
 * Componentes a implementar:
 *   - AppShell.tsx       → Layout principal con sidebar y header
 *   - AuthLayout.tsx     → Layout para pantallas de login/registro (centrado)
 *   - Header.tsx         → Barra superior con navegación y perfil
 *   - Sidebar.tsx        → Menú lateral de navegación
 */

export { AppShell } from './AppShell';
export { Sidebar } from './Sidebar';
export { AuthLayout } from './AuthLayout';
