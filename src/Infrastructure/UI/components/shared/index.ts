/**
 * UI LAYER - Shared Components
 * ─────────────────────────────────────────────────────────────────────────────
 * Componentes reutilizables en toda la aplicación (sin lógica de negocio).
 *
 * Componentes a implementar:
 *   - Button.tsx         → Botón con variantes (primary, secondary, ghost)
 *   - Input.tsx          → Input con label y mensajes de error
 *   - Spinner.tsx        → Indicador de carga
 *   - Alert.tsx          → Mensajes de error/éxito
 *   - Card.tsx           → Contenedor con sombra y borde
 *   - Modal.tsx          → Diálogo modal reutilizable
 */

export { Button } from './Button';
export { Input } from './Input';
export { Card } from './Card';
export { Alert } from './Alert';
export { Spinner } from './Spinner';
